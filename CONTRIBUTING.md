# المساهمة في Disor

شكراً إنك عايز تساهم! 🎉

## إزاي تساهم؟
1. **Fork** المشروع
2. اعمل **Branch** جديدة: `git checkout -b feature/اسم-الميزة`
3. عدّل الكود وأضف ميزتك
4. **اختبر** إن الكود شغال: `python -m py_compile *.py`
5. اعمل **Commit** واضح: `git commit -m "إضافة مهارة كذا"`
6. اعمل **Push** وافتح **Pull Request**

## قواعد مهمة
- **مفيش أسرار في الكود**: التوكن والمفاتيح دايمًا في `data.json` أو متغيرات البيئة (الملف في `.gitignore`)
- **مفيش `administrator` تلقائيًا**: أي صلاحيات بتطلع من الموديل بتمشي على `sanitize_perms`
- **كل مهارة جديدة محتاجة**:
  - مُعرّفة في `SKILL_SCHEMA` (ai.py)
  - مُعرّفة في `SKILL_PERM_MAP` بالصلاحية المطلوبة (skills.py)
  - دالة تنفيذ جديدة + توزيعها في `execute()` (skills.py)
  - مثال (few-shot) في `parse()` (ai.py)
- الكود يفضل بالعامية المصرية في الرسائل والتعليقات عشان الهوية بتاعة البوت

## تقارير المشاكل
افتح **Issue** ووضح: اللي حصل، اللي كان متوقع، وخطوات إعادة إنتاج المشكلة.
